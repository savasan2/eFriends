<% unless current_user.following?(user) %>
  <%= link_to follow_search_path(user.id), method: :POST, class: "btn btn-lg btn-outline-danger fw-bold  bg-transparent ", remote: true do %>
    <i class="fas fa-heart" style="color: red"></i>
  <% end %>
<% end %>